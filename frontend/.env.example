# =============================================================================
# SmartCinema Frontend Environment Variables
# =============================================================================

# In local development: leave empty to let Vite proxy (/api) handle requests
# In production deployment (e.g. Vercel, Netlify): set this to your hosted backend URL
# Example: VITE_API_URL=https://smartcinema-backend.onrender.com
VITE_API_URL=
